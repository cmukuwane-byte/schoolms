spring.application.name=schoolms
server.port=8084

# ---- MS SQL Server ----
spring.datasource.url=jdbc:sqlserver://localhost:1433;databaseName=SchoolMS;encrypt=true;trustServerCertificate=true
spring.datasource.username=sa
spring.datasource.password=Mkwn@011255
spring.datasource.driver-class-name=com.microsoft.sqlserver.jdbc.SQLServerDriver

spring.jpa.hibernate.ddl-auto=update
# Kept open: the Thymeleaf views walk lazy associations while rendering.
spring.jpa.open-in-view=true
spring.jpa.properties.hibernate.format_sql=true

spring.thymeleaf.cache=false

# ---- Uploads ----
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=10MB
app.upload-dir=./uploads

# ---- Mail ----
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=cmukuwane@gmail.com
spring.mail.password=sppp iqtq gpsi sphm
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# ---- Library rules ----
app.library.loan-days=14
app.library.max-loans-student=3
app.library.max-loans-staff=6
app.library.fine-per-day=0.50

# ---- Bootstrap administrator (created only on an empty user table) ----
app.bootstrap.admin-username=admin
app.bootstrap.admin-password=Admin#2026
